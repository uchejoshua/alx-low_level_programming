#include <stdio.h>

/**
 * main - prints all single numbers of base 10
 * Return: 0 executed successfully
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");

	return (0);
}
